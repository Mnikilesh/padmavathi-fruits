{
  "name": "padmavathi-fruits",
  "version": "2.0.0",
  "description": "Padmavathi Fruits Company — Warangal | Netlify + MongoDB Atlas",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build step — static frontend'",
    "seed": "node scripts/seed.js",
    "lint": "echo 'No linter configured'"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "busboy": "^1.6.0",
    "cloudinary": "^2.2.0",
    "dotenv": "^17.3.1",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.0.3",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
